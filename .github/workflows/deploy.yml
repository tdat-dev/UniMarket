name: 🚀 Deploy to aaPanel via FTP
on:
  push:
    branches:
      - main # hoặc master - branch chính của bạn
jobs:
  deploy:
    name: 📦 Deploy
    runs-on: ubuntu-latest

    steps:
      - name: 🔄 Checkout code
        uses: actions/checkout@v4
      - name: 📤 Upload via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./ # Thư mục local cần upload
          server-dir: /www/wwwroot/zoldify.com # Thư mục đích trên server
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
            **/.env
            **/README.md
